---
label: News
hide_body: false
fields:
- name: add_image
  type: file
  config:
    maxSize: 64
  label: Add image
- name: add_text
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: add text
- name: cbc
  type: field_group_list
  config:
    min: 
    max: 
    labelField: 
  label: cbc
- name: block
  type: blocks
  label: block
  template_types: []
  config:
    min: 
    max: 
pages:
- content/posts/test123.md
